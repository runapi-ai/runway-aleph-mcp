{
  "servers": {
    "runway-aleph": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@runapi.ai/runway-aleph-mcp"
      ],
      "env": {
        "RUNAPI_API_KEY": "${RUNAPI_API_KEY}"
      }
    }
  }
}
